services:
  upnptube:
    container_name: upnp-tube
    image: node:20-bookworm
    network_mode: host
    restart: unless-stopped

    working_dir: /app

    volumes:
      - ./upnpTube:/app
      - ./start.sh:/start.sh

    command: bash /start.sh
